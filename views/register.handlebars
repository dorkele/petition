{{!-- conditionally renders an error message --}}
<h1>Petition to stop signing petitions</h1>
<div>Please register first.</div>
<form method="POST">
    {{#if err}} <div>O-ou, something went wrong. We are very sorry, please try again!</div> {{/if}}
    <p>First name</p>
    <input type="text" id="first" name="first">
    <p>Last name</p>
    <input type="text" id="last" name="last">
    <p>Email</p>
    <input type="text" id="email" name="email">
    <p>Password</p>
    <input type="text" id="password" name="password">
    <div><button id="button">Submit</button></div>
     
     <a href="login">Already registered?</a>
     {{!-- <input type="hidden" name="_csrf" value="{{csrfToken}}">
    {{!-- add this form field in every form i will have!!!!!! --}}
</form>